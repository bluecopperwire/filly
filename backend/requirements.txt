# FILLY Backend Dependencies
# NOTE: Using >= constraints for Python 3.14 compatibility.
# Exact pinned versions (pydantic==2.10.0) fail to build pydantic-core
# on Python 3.14 due to PyO3 version limits.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-multipart>=0.0.18
python-docx>=1.1.2
aiofiles>=24.1.0

# ── NLP (Milestone 3 — deferred until GECToR integration) ──
# torch>=2.0.0
# transformers>=4.40.0
# pyxdameraulevenshtein>=1.7.1
